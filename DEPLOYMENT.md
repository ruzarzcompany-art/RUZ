# تشخيص وإصلاح مشكلة النشر (قاعدة البيانات غير جاهزة)

آخر تحديث: 2026-07-28

## الحالة الحالية (اقرأ هذا أولاً)

السبب الجذري **تأكّد وحُلّ جزئياً**: الموقع لم يكن مربوطاً بمستودع GitHub
إطلاقاً، وكل نشر سابق تمّ عبر Netlify CLI من بيئة الوكيل دون أي `commit`
إلى git. بعد أن ربطتَ الموقع بالمستودع ونفّذت أول نشر إنتاجي من GitHub:

- ✅ أُنشئ فرع قاعدة البيانات الإنتاجي فعلاً (وهو ما كان مفقوداً).
- ❌ لكن ما نُشر هو **الملفات القديمة في `main`** فقط، لأن كود الوكيل لم
  يكن مرفوعاً إلى git بعد — فظهر الموقع 404.

**المتبقّي**: مزامنة الكود الحالي مع الفرع `main` في
`github.com/ruzarzcompany-art/RUZ`، ثم ترك Netlify ينشر تلقائياً. بيئة
الوكيل **لا تستطيع** الدفع إلى git (أوامر `git push` / `remote` / `config`
معطّلة، ولا يوجد `gh` أو رمز وصول)، لذلك تُسلَّم نسخة الكود كأرشيف مصدري
وتُنفَّذ المزامنة من جهازك — التفاصيل في قسم «مزامنة الكود مع GitHub».

بعد الدفع، سيطبّق النشر التالي **أربعة ترحيلات** بالترتيب:

| الترحيل | المحتوى |
| --- | --- |
| `20260727130142_create_hr_core_tables` | الجداول الأساسية (موظفون، فروع، حضور، أدوار، جلسات) |
| `20260727135527_hr_forms_face_shifts` | قوالب الوجه، التدقيق، نماذج الموارد البشرية والرواتب |
| `20260727155155_employee_file_schedules_overrides` | ملف الموظف، جداول الدوام، مدير الفرع، الصلاحيات الفردية |
| `20260728001201_printables_cashier_inventory_settings` | إعدادات الشركة، الأقسام والمسميات وبنود الرواتب، تقفيل الكاشير، المخزون، الإنذارات، سجل النماذج المُصدرة |

## مزامنة الكود مع GitHub

الأرشيف المصدري (66 ملفاً، بلا `node_modules` ولا `.git` ولا أي أسرار —
وحقل `repository` في `package.json` مُنظَّف من أي رمز وصول) موجود على
الموقع تحت:

```
/_source/restaurant-hr-source-848aa5e17cd3.tgz
```

من جهازك:

```bash
# 1) نزّل الأرشيف من رابط الموقع ثم فُكّه في مجلد مؤقّت
curl -O "<رابط الموقع>/_source/restaurant-hr-source-848aa5e17cd3.tgz"
mkdir -p ruz-sync && tar -xzf restaurant-hr-source-848aa5e17cd3.tgz -C ruz-sync

# 2) انسخ محتواه فوق نسخة مستودعك المحلي
rsync -a --exclude .git ruz-sync/ /path/to/RUZ/

# 3) ادفع
cd /path/to/RUZ
git add -A
git commit -m "sync: restaurant-hr full application"
git push origin main
```

> **بعد نجاح المزامنة احذف مجلد `public/_source/` من المستودع وادفع
> الحذف** — لا داعي لنشر أرشيف الكود على موقع عام.

تأكّد أيضاً أن **Production branch** في
**Project configuration → Build & deploy → Continuous deployment →
Branches and deploy contexts** هو نفس الفرع الذي تدفع إليه (`main`).

---

## التشخيص الأصلي (محفوظ للرجوع)

## الأعراض

- كل مسار يعتمد على قاعدة البيانات (`POST /api/auth/login`، الحضور، لوحة الموارد البشرية، التقارير) يُرجع:
  `503 {"ok":false,"error":"قاعدة البيانات غير جاهزة. تأكد من تنفيذ الترحيلات ومن صحة رابط الاتصال (DATABASE_URL)."}`
- كل عملية نشر تنتهي بحالة `state: "error"` والرسالة:
  `Database migration failed: branch for deploy not found` مع `database_branch_id: "main"`.
- في المقابل: `GET /api/health` يعمل (200)، و`/app/` يُحمَّل (200) — أي أن النشر نفسه يصل والملفات تُخدَم،
  والخلل محصور في ربط قاعدة البيانات.

## السبب الجذري

> **تحديث 2026-07-28**: تأكّد لاحقاً أن السبب وراء غياب النشر الإنتاجي هو
> أن الموقع لم يكن مربوطاً بمستودع GitHub أصلاً (`Not linked`). ما يلي هو
> التشخيص كما كان وقت الفحص.

1. المشروع **لم يُنشر إلى الإنتاج ولا مرّة واحدة**: بيانات الموقع تُظهر `published_deploy: null`.
2. Netlify Database (Neon) تُنشئ فرع قاعدة البيانات الإنتاجي (`main`) عند أول نشر إنتاجي،
   ثم كل Deploy Preview يكون نسخة (fork) من هذا الفرع.
3. لأن فرع `main` غير موجود، فإن مُطبِّق الترحيلات في المنصّة يبحث عن فرع قاعدة البيانات الخاص بعملية
   النشر (`database_branch_id: "main"`) فلا يجده، فيفشل بالرسالة `branch for deploy not found`.
4. لأن خطوة الترحيلات تفشل، **لا تُضخّ المنصّة أي بيانات اتصال إلى الدالة**؛ تشخيص مؤقّت داخل الدالة
   أثبت أن `NETLIFY_DB_URL` و`NETLIFY_DATABASE_URL` و`DATABASE_URL` كلها غير موجودة في وقت التشغيل.
   لذلك يفشل `ensureSeeded()` ويُرجع الوسيط رسالة «قاعدة البيانات غير جاهزة».

أي أن الرسالة العربية نتيجة وليست سبباً: الترحيلات والرابط سليمان في المستودع، لكن المنصّة لم تسلّم رابطاً.

## ما يُثبت أن الخلل من جهة المنصّة لا من الكود

- نشر بعد **حذف الترحيل المعلّق مؤقتاً** (بقي الترحيلان المطبّقان فقط): نفس الفشل.
- نشر مع `BRANCH=main` ليتطابق اسم الفرع المحلي (`master`) مع فرع المشروع (`main`):
  سجل البناء صار `Creating database branch for main`، ومع ذلك نفس الفشل.
- نشر بلا بناء إطلاقاً (`netlify deploy --no-build`): **نفس الفشل** — أي أن الخطوة الفاشلة تُنفَّذ
  في خطّ أنابيب النشر على السيرفر، ولا يمكن لأي خيار محلي تغييرها.
- `netlify db status` يفشل بـ `TypeError: Cannot read properties of null` أي أن بيانات قاعدة البيانات
  الخاصة بالمشروع غير قابلة للقراءة من الـCLI، و`netlify env:list` يعيد `{}` (لا متغيرات على مستوى الموقع).
- محاولات إصلاح من داخل صلاحيات الوكيل مرفوضة من المنصّة:
  `netlify deploy --prod` → `Forbidden`، `netlify deploy --trigger` → `Unauthorized`،
  `netlify api createSiteBuild` → `Unauthorized`. الرمز المتاح للوكيل صلاحيته نشر معاينة فقط.

## الإجراء المطلوب منك (خارج صلاحيات الوكيل)

1. افتح لوحة Netlify: `https://app.netlify.com/projects/taupe-sprite-321915`
2. من القائمة الجانبية اختر **Deploys**، ثم من الزر المنسدل **Trigger deploy** اختر
   **Clear cache and deploy site**. هذا أول نشر إنتاجي (context = production) وهو ما يُنشئ فرع
   قاعدة البيانات `main` ويطبّق الترحيلات الثلاثة عليه.
3. تأكّد قبل ذلك أن فرع الإنتاج صحيح: **Project configuration → Build & deploy → Continuous deployment
   → Branches and deploy contexts**، وقيمة **Production branch** يجب أن تكون الفرع الذي تُرفَع إليه
   تعديلات الوكيل فعلياً. إعداد المشروع الحالي `repo_branch: "main"` بينما الفرع المحلي في بيئة الوكيل
   اسمه `master`؛ إن كانت التعديلات تُدفَع إلى `master` فلن يشتغل أي نشر تلقائي عبر git حتى تُصحَّح القيمة.
4. إن فشل النشر الإنتاجي بنفس الرسالة، فالمشكلة في ربط الإضافة نفسها:
   **Extensions → Netlify Database (Neon)** (أو **Database** في القائمة الجانبية) → أعد الربط
   عبر **Provision/Connect database**، أو افتح مشروع Neon من نفس الصفحة وأنشئ فرعاً باسم `main`
   من الفرع الموجود، ثم أعِد **Trigger deploy**.

## التحقق بعد تنفيذ الإجراء

```bash
# 1) الرابط الإنتاجي
curl -s https://taupe-sprite-321915.netlify.app/api/health

# 2) تسجيل الدخول (يجب أن يعيد ok:true ورمز token)
curl -s -X POST https://taupe-sprite-321915.netlify.app/api/auth/login \
  -H 'content-type: application/json' \
  -d '{"identifier":"EMP-1002","password":"Restaurant@2026"}'
```

بيانات الدخول التجريبية تُبذَر تلقائياً عند أول طلب ناجح (`ensureSeeded`).
كلمة المرور الافتراضية تأتي من `SEED_DEFAULT_PASSWORD` وقيمتها الافتراضية `Restaurant@2026`.

## ما تحقّقتُ منه فعلياً

- `EMP-1002` موجود في قاعدة البيانات، حالته `is_active = true`، وتحقّق كلمة المرور الافتراضية
  مقابل التجزئة المخزّنة يُرجع `true` (فُحص عبر اتصال قراءة فقط داخل بيئة الوكيل).
- على آخر نشر معاينة: `/api/health` → 200، `/app/` → 200، `POST /api/auth/login` → 503 برسالة
  «قاعدة البيانات غير جاهزة» — أي أن المتبقّي الوحيد هو ضخّ رابط قاعدة البيانات من المنصّة.
- **لم أتمكّن من إتمام تسجيل دخول حقيقي عبر الشبكة**، لأن تسجيل الدخول يحتاج كتابة (جلسة جديدة +
  تحديث `last_login_at`) واتصال الوكيل بقاعدة البيانات للقراءة فقط، والنشر لا يستلم أي رابط اتصال.
  التحقق النهائي يصبح ممكناً فور تنفيذ الإجراء أعلاه.
